# frozen_string_literal: true

source "https://rubygems.org" do 
    # Start gems
    gem "jekyll"
    gem "jekyll-paginate"
    # Avoid polling for changes
    gem 'wdm', '>= 0.1.0' if Gem.win_platform?
end

git_source(:github) {|repo_name| "https://github.com/awocallaghan/awocallaghan.github.io" }

